# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code state
.claude/scheduled_tasks.lock

# Supabase credentials and local dumps. The anon key is publishable, but the
# database password and service_role key are not, and a stray .dump is a
# complete copy of every kid's progress.
.env
.env.local
*.dump
